1. clone repository
2. make new schema in mysql workbench, then use database.sql.
3. configure config.json file for your database server
4. Running the program
    find ui.exe in src -> dist and put it in the same folder with config.json
    then run ui.exe
